---
upgrade:
  - |
    ``OS::Manila::Share`` attribute ``export_locations`` will now be a list of
    export paths
fixes:
  - |
    ``OS::Manila::Share`` attribute ``export_locations`` will now be a list of
    export paths
